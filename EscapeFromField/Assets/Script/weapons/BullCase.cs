using UnityEngine;
using UnityEngine.SceneManagement;

public class BullCase : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Destroy(gameObject, 5);
    }
}
